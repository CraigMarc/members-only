extends layout

block content
  
  div(class="buttonsCenter")
    div(class="buttonContainer")
      h1= title
      div(class="buttonDiv")
        <a href="/message/sign-in">Log In</a>
      div(class="buttonDiv")
        <a href="/message/create">Sign Up</a>

  h2 Messages
  

  dl
    each allMessages in message
      dt
        div(class='messageContainer')
          p(class="messageTitle") #{allMessages.title}
          p #{allMessages.text}

    else
      p There are no messages.
